[tox]
envlist = py26, py27, py33, py34, py35, py36

[testenv]
commands = coverage run --source flowpipe -m pytest --cov=flowpipe --cov-report=term tests/ --cov-config .coveragerc
deps =
    pytest
    pytest-cov
    mock
    codeclimate-test-reporter
    codacy-coverage
    doxypypy
